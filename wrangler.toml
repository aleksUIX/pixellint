name = "pixellint-site"
compatibility_date = "2026-07-26"
workers_dev = true

# The playground is static: HTML, CSS, one ES module, and the WASM build. No
# server code, so no Worker script.
assets = { directory = "./site" }

routes = [{ pattern = "pixellint.org", custom_domain = true }]
