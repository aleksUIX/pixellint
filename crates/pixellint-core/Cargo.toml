[package]
name = "pixellint-core"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
description = "Pixellint core: spec-backed validation engine and declarative rulepacks for pixels, postbacks, and tracking URLs"
keywords = ["pixel", "adtech", "tracking", "validator", "linter"]
categories = ["parser-implementations", "development-tools"]
readme = "../../README.md"

[dependencies]
regex = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
url = "2.5"
